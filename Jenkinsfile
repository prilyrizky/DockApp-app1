pipeline {
    agent none
    stages {
        stage('Build Image'){
            agent {dockerfile true}
            steps{
                echo 'build image docker berhasil'
            }
        }  
    }
}
